# ReconForge V2 Dependencies
# anthropic removed — replaced by openai SDK pointing at NIM + Groq

openai>=1.0.0          # OpenAI-compatible SDK used for NIM + Groq
httpx>=0.27.0          # Async HTTP client used by AI red-team agent
python-whois>=0.9.0    # Deterministic whois parser
chromadb>=0.5.0
click>=8.1.0
pyyaml>=6.0
sqlite-utils>=3.35
rich>=13.0
tenacity>=8.2
python-dotenv>=1.0
jinja2>=3.1
pydantic>=2.0
pytest>=7.0
pytest-asyncio>=0.21.0
playwright>=1.40.0
